server:
  port: ${PORT:8888}
spring:
  application:
      name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URI}
          default-label: main
          search-paths:
            - ${PATH}